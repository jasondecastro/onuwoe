<!-- troublemaker: switch 2 other cards (checkboxes, not their own) -->
<!--  {remote: true, html: {class: "show_card", 'data-type' => 'json'} -->

<%= form_for @game do |f| %>
  <%= f.label :players %><br>
  <%= f.collection_check_boxes :players, @game.players.all, :id, :nickname %>
  <%= f.submit %>
  <% end %>

